# KCMS 입학관리 디자인 시스템

기준 파일: PCMS 입학관리 화면 체험판 · 기준 화면 **PCMS-SCR-ET-02-012 예비생 등록·관리**
최종 정리 2026-08-26

---

## 1. 레이아웃 골격

| 영역 | 값 |
|---|---|
| 상단 GNB (`.topnav`) | 높이 **64px**, `#3A5170 → #2C3E5A` 세로 그라디언트 |
| 좌측 아이콘 레일 (`.rail`) | 폭 **58px** (1000px 이하 50px), 배경 `#F4F6F9` |
| 히스토리 네비 (`.bcbar`) | 높이 **30px**, 흰 배경, 하단 1px |
| 하단 작업 탭 (`.worktabs`) | 높이 **30px**, 배경 `#E5EAF1`, 탭 높이 24px |
| 콘텐츠 기준선 | 콘텐츠 박스 **73px**, 안쪽 여백 14px |

## 2. 색 스케일

```
브랜드   --pb #0066FF · --pb-light #E8F0FF · --pb-dark #0052CC · --pb-deeper #003D99
GNB     --gnb #2C3E5A · --gnb-top #3A5170 · --gnb-deep #1B2536
중립    --txt #1F2937 · --txt-mut #5B6776 · --line #CFD6E0 · --line-soft #E3E8EF · --bg #EEF1F5
표      --tbl-head #F4F7FD · --tbl-head-grp #EAF1FB · --tbl-bd #C8D5EB
상태    --warn #F97316 · --danger #E5484D
그래프   --chart-ok #5E93EE · --chart-low #E58388
```

**면적에 따른 색 선택**

| | 그래프 (면적 넓음) | 글자·아이콘 (면적 좁음) |
|---|---|---|
| 기준 이상 | `#5E93EE` | `#0066FF` |
| 기준 미만(경고) | `#E58388` | `#E5484D` |

## 3. 타이포

| 용도 | 크기 / 굵기 |
|---|---|
| 섹션 제목 | 13px / 700 (앞에 4×14 파란 막대) |
| 탭·강조 라벨 | 12.5px / 700 |
| 표·폼 본문 | 11.5px / 400 |
| 배지·캡션 | 11px / 500 |
| 지표 수치 | 22~34px / 700, `%` 기호만 0.68em |

## 4. 컴포넌트 스펙

### 버튼
| 클래스 | 용도 | 배경 |
|---|---|---|
| `.btn` | 주요 동작 (검색·저장·등록) | `--pb` |
| `.btn.g` | 보조 (초기화·닫기) | 흰색 |
| `.btn.s` | 시스템 (엑셀·인쇄) | `--gnb` |
| `.btn.danger` | 파괴적 동작 | `--danger` |
| `.btn.sm` | 표 안 소형 | 높이 22px |

### 배지
`.badge` 기본 / `.primary` `.ok` `.warn` `.danger` `.line` / `.pill` 알약형 — 굵기 500 고정

### 표
헤더 33px `#F4F7FD` 11.5/700 · 2단 그룹 헤더 `.grp` `#EAF1FB` · 격자선 `#C8D5EB`
숫자 칸 `.num` 우측 정렬 + `tabular-nums` · 합계 행 `tfoot`

**의도된 예외** — 되돌리지 말 것

| 화면 | 값 | 이유 |
|---|---|---|
| 진학생 관리 | 헤더 42px | 학기 표기 보조 라벨이 붙는 2줄 헤더 |
| 예비생정보 / 등록·관리 | `stattbl` 66px | 2줄 헤더 |
| 반편성 배치관리 | `matrix` 48px | 학급 카드가 들어가는 보드형 |
| 예비생 등록(유치부) | 헤더 3톤 | 레벨/등록·잔여/대기 칸 성격 구분 |

### 모달
`.modal-ov > .modal-box > (.modal-hd 40px + .modal-body + .modal-ft)`
기본 폭 640px · 헤더 드래그 이동 · Esc·오버레이 클릭으로 닫힘

**모달 인벤토리 (총 44종)** — 공통 26종(스크립트 제공 10 + 반복 정의 16) · 화면 전용 18종

### 토스트
4색(success / warning / danger / neutral) · 우하단 스택 최대 3개
왼쪽 액센트 바 금지 · 문구에 이모지 금지

### 달력
오늘 = `#0066FF` 원 채움 + 흰 글자 / 선택일 = 파란 1px 링 + 파란 글자
파랑은 `#0066FF` 한 가지만 쓰고, 구분은 색이 아니라 형태(채움 ↔ 링)로 한다.

## 5. GNB 메뉴 정렬

```
1뎁스 탭 글자        ┃ 기준선
2뎁스 카테고리 글자   ┃ 같은 기준선
3뎁스 항목 점        ┃ 기준선 +3.2px  (점 중심 = 2뎁스 첫 글자 중심 +5.2px)
3뎁스 항목 글자      ┃ 점 4px + 간격 6px 만큼 들여쓰기
```

드롭다운 박스는 탭 박스와 좌측 경계가 정확히 일치한다(`boundingRect.left` 를 반올림하지 않는다).
드롭다운 여백: 박스 `14px 18px` · 열 간격 `18px 26px` · 열 최소 폭 `118px`.

## 6. 반응형

| 브레이크포인트 | 변화 |
|---|---|
| 1300px 이하 | 지표·차트 1열 |
| 1100px 이하 | 검색 필터 줄바꿈 |
| 1000px 이하 | 좌측 레일 58 → 50px |

## 7. 대시보드는 예외

메인 지표 화면이라 여백·타이포를 별도로 잡았습니다. 업무 화면의 기준으로 삼지 마세요.

```
카드 안쪽 여백 40px · 도넛 132px
제목 17px · 큰 수치 34px · 설명 14.5px
지표블럭(회색 라운드) 라벨 11px / 값 15px  ← 확대하지 않음
```
